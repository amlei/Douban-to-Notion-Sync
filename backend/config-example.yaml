# provider: qq / outlook / 163 / 126 / yeah / custom
# For presets, only username and password are required.
# For "custom", fill in host/port/use_ssl manually.
smtp:
  provider: "qq"
  username: ""
  password: ""
  # from_email defaults to username if empty
  # from_email: ""
  # from_name: LifeInk AI
  # Override preset (leave empty to use preset defaults):
  # host: ""
  # port: 0
  # use_tls: false
  # use_ssl: false

redis:
  host: "localhost"
  port: 6379
  db: 0
  password: ""

postgres:
  host: "localhost"
  port: 5432
  user: "lifeink"
  password: ""
  dbname: "lifeink"
  sslmode: "disable"

# JWT secret -- auto-generated on first startup if left empty.
# jwt_secret: ""

# Python scraper service URL
scraper_url: "http://127.0.0.1:50051"
